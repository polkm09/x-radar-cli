# Current Status

Last updated: 2026-07-01

## Formal Project Location

- Workspace root: `$HOME/Downloads/自媒体开发`
- Source Git repository: `$HOME/Downloads/自媒体开发/topic-radar`
- Archived pre-formalization root: recorded in `$HOME/Downloads/自媒体开发/.previous_root_archive`

Future development defaults to the source Git repository above.

## Current Baseline

- Baseline version: `0.1.110`
- Package name in source: `topic-radar`
- Released CLI packages:
  - `topic-collector`
  - `topic-vertical`

`topic-radar` is the source repository name and build workspace. It is not the desired deployment package name.

## Deployment Outputs

Deployment packages are generated by:

```bash
node scripts/prepare-deployment.mjs
```

Expected output folders:

- `$HOME/Downloads/自媒体开发/数据采集工具/topic-collector-0.1.110`
- `$HOME/Downloads/自媒体开发/垂直领域发现/topic-vertical-0.1.110`

## Verified Deployment Machine State

Most recent user-reported Mac mini state:

- `topic-collector --version` returned `0.1.108`
- `topic-vertical --version` returned `0.1.108`
- `opencli doctor` passed on OpenCLI `1.8.3`
- OpenCLI Chrome extension connected
- `lark-cli doctor` passed after update and login, with user identity ready

Re-verify before relying on this state for new deployment work.

## Important Known Constraints

- Xiaohongshu had recovered, but access frequency must stay conservative.
- Douyin comments use the documented DOM path, not the old unstable API fallback.
- `topic-vertical` is strategy/statistics/analysis. Direct platform page collection belongs in `topic-collector`.
- DeepSeek is required for formal `topic-vertical` plan review unless an explicit debug flag allows a rule-only plan.
- `config/deepseek.key` may exist locally for packaging convenience, but it is ignored by Git.
